import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { ArrowLeft, CheckCircle2 } from 'lucide-react'; import { Field, Button, Notice } from '../lib/ui.jsx'; import { useCanal } from '../context/CanalContext.jsx';
export default function Consulta(){const nav=useNavigate();const {registrarCaso}=useCanal();const [enviado,setEnviado]=useState(false);const [codigo,setCodigo]=useState('');const [form,setForm]=useState({nombre:'',contacto:'',motivo:'',descripcion:''});const set=(k,v)=>setForm({...form,[k]:v});function submit(e){e.preventDefault();setCodigo(registrarCaso('Consulta',form));setEnviado(true)}if(enviado)return <div className="mx-auto max-w-2xl space-y-6"><Notice title="Consulta recibida">La maqueta generó un código de seguimiento para simular el flujo.</Notice><div className="rounded-3xl border border-slate-200 bg-white p-8 text-center shadow-sm"><CheckCircle2 className="mx-auto text-verde-700" size={48}/><p className="mt-4 text-sm text-slate-500">Código de seguimiento</p><div className="mt-2 text-3xl font-bold tracking-wide">{codigo}</div><div className="mt-6 flex justify-center gap-3"><Button variant="secondary" onClick={()=>nav('/')}>Volver al inicio</Button><Button onClick={()=>nav(`/seguimiento?codigo=${codigo}`)}>Ver seguimiento</Button></div></div></div>;return <div className="mx-auto max-w-3xl space-y-6"><button onClick={()=>nav('/')} className="flex items-center gap-2 text-sm font-semibold text-verde-700"><ArrowLeft size={16}/> Volver al inicio</button><div><h1 className="mt-5 text-3xl font-bold text-slate-900">Realizar una consulta</h1><p className="mt-2 text-sm leading-6 text-slate-600">Formulario exploratorio para consultas relacionadas con integridad.</p></div><Notice title="Antes de comenzar">No ingreses información real en esta maqueta. La diferencia entre consulta y denuncia deberá validarse durante el levantamiento.</Notice><form onSubmit={submit} className="space-y-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid gap-5 sm:grid-cols-2"><Field label="Nombre"><input value={form.nombre} onChange={e=>set('nombre',e.target.value)} className="w-full rounded-xl border border-slate-300 px-3 py-2.5" placeholder="Nombre y apellido"/></Field><Field label="Correo o medio de contacto"><input value={form.contacto} onChange={e=>set('contacto',e.target.value)} className="w-full rounded-xl border border-slate-300 px-3 py-2.5" placeholder="correo@ejemplo.cl"/></Field></div><Field label="Motivo de la consulta" required><select required value={form.motivo} onChange={e=>set('motivo',e.target.value)} className="w-full rounded-xl border border-slate-300 px-3 py-2.5"><option value="">Selecciona una opción</option><option>Orientación sobre integridad</option><option>Consulta sobre procedimiento</option><option>Consulta normativa</option><option>Otro</option></select></Field><Field label="Descripción de la consulta" required><textarea required value={form.descripcion} onChange={e=>set('descripcion',e.target.value)} rows="7" className="w-full rounded-xl border border-slate-300 px-3 py-2.5" placeholder="Describe tu consulta..."/></Field><div className="flex flex-col-reverse gap-3 border-t pt-5 sm:flex-row sm:justify-between"><Button type="button" variant="secondary" onClick={()=>nav('/')}>Cancelar</Button><Button type="submit">Enviar consulta</Button></div></form></div>}
